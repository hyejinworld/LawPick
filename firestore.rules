rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // reviews 컬렉션 규칙
    match /reviews/{document=**} {
      // 누구나 리뷰를 읽을 수 있음
      allow read: if true;
      // 로그인한 사용자만 리뷰를 작성/수정할 수 있음
      allow write: if request.auth != null;
    }
  }
}
